[project]
name = "colab-gpu-bridge"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "ogoromob", email = "genspark_dev@genspark.ai" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
colab-gpu-bridge = "colab_gpu_bridge:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
